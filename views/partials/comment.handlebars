<div class="card">
    <div class="card-header">
        <h5>{{title}}</h5>
        <p>{{updatedAt}}</p>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-10">
                {{body}}
            </div>
            <div class="col-2">
                <button type="button" class="btn btn-danger deleteComment" postId={{_id}}>Delete</button>
            </div>
        </div>
    </div>

</div>